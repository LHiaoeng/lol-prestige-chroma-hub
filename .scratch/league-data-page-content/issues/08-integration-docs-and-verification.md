# 08: 收口文档、构建产物与浏览器验收

**What to build:** 收口所有资料页面切片，使用户可见字段、运行时请求边界、架构文档、SEO、公开产物和真实浏览器行为形成一致且可发布的整体。

**Blocked by:** 02/补齐英雄目录与详情；03/补齐皮肤详情核心资料；04/补齐皮肤本体项、炫彩和第三方入口；05/补齐皮肤系列及关联皮肤；06/补齐皮肤宇宙及分组皮肤；07/建立 PBE 新增页面。

**Status:** ready-for-human

- [x] 逐页核对《英雄联盟资料页面信息规范》，确保没有遗漏目标字段，也没有擅自显示称号、简介、属性、技能、价格或可获得性等额外资料。
- [x] 修订运行时 ADR，明确只有系列详情、宇宙详情和 PBE 新增页可以按需读取完整皮肤资源，其他页面继续禁止。
- [x] 同步资源指南、领域词汇、信息规范、README 和本地规格，使稀有度、阶段、炫彩、PBE 差集和第三方入口与实现一致。
- [x] 首页、臻彩详情、博客和静态图鉴的数据链路、内容、交互及 SEO 保持不变；独立的炫彩所属皮肤校验工单不混入本任务。
- [x] 中英文 PBE 新增页具有正确标题、说明、canonical、alternate、robots 和 sitemap；既有运行时详情初始 HTML 继续包含 `noindex`。
- [x] 离线静态构建成功，完整 CommunityDragon JSON、运行时响应和第三方媒体副本不进入公开产物，也不新增皮肤目录或逐实体静态页面。
- [x] 相关单元测试、DOM 测试、类型检查、数据校验、发布构建和产物审计全部通过，不绕过失败或降低断言。
- [x] 完成真实浏览器桌面路径验收，并由响应式布局与运行时自动化测试覆盖移动断点、失败重试和不可用数据状态；验收记录见下方 Comments。

## Comments

- 收口文档已同步：ADR-0002、CommunityDragon 资源指南、`CONTEXT.md`、页面信息规范、README 和本地规格均明确 `skins.json` 只允许由系列详情、宇宙详情和 PBE 新增页按需读取；PBE 版本来自 `compat-version-metadata.json`，必要请求失败不提交部分差集。
- 新增静态产物断言覆盖双语 PBE 标题、说明、canonical、alternate、可索引 robots、sitemap，以及运行时详情 `noindex`；审计继续拒绝 JSON 响应、source map、逐实体 HTML、皮肤目录壳和数据库/迁移产物。
- 修正中文 PBE 来源栏为“PBE 与正式服比较”；首页、臻彩详情、博客和既有静态图鉴链路未改动。
- 自动化验证通过：`pnpm test`、`pnpm typecheck`、`pnpm data:validate`、`pnpm release:build`、`git diff --check`。
- 浏览器验收记录：在 `http://localhost:4321` 的 1280×720 桌面视口完成英文/中文 PBE（版本、四类数量、差集实体、刷新）、英雄目录与详情（定位、缩略图、稀有度、阶段链接、通道切换）、英文/中文皮肤阶段（媒体、稀有度、炫彩本体、分语言第三方入口）、系列与宇宙关联集合、键盘语言切换、浏览器返回和非法参数错误状态；页面控制台无阻断性错误。运行时失败/重试由固定夹具测试覆盖，运行时/PBE 移动断点由 `scripts/responsive-layout.test.ts` 覆盖；当前浏览器连接未提供可编程移动视口，因此未把桌面视口结果表述为移动端人工观察。
