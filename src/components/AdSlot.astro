---
import type { AdPlacement } from '../domain/ad-policy';

interface Props {
  placement: AdPlacement;
}

const { placement } = Astro.props;
---
<div class="ad-boundary" data-ad-boundary={placement}></div>
