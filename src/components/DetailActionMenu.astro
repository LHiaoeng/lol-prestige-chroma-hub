---
export interface DetailAction {
  labelEn: string;
  labelZh: string;
  href: string;
}

interface Props {
  actions: DetailAction[];
}

const { actions } = Astro.props;
---
{actions.length === 1 ? (
  <a class="detail-action-link" href={actions[0].href} target="_blank" rel="noopener noreferrer" aria-label={actions[0].labelEn} data-aria-en={actions[0].labelEn} data-aria-zh={actions[0].labelZh} title={actions[0].labelEn}>
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M14 4h6v6M20 4l-9 9M18 13v6a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h6" />
    </svg>
  </a>
) : actions.length > 1 && (
  <details class="detail-action-menu">
    <summary aria-label="External actions" data-aria-en="External actions" data-aria-zh="外部操作" title="External actions">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M14 4h6v6M20 4l-9 9M18 13v6a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h6" />
      </svg>
    </summary>
    <nav class="detail-action-list" aria-label="External actions" data-aria-en="External actions" data-aria-zh="外部操作">
      {actions.map((action) => (
        <a href={action.href} target="_blank" rel="noopener noreferrer" aria-label={action.labelEn} data-aria-en={action.labelEn} data-aria-zh={action.labelZh} data-en={action.labelEn} data-zh={action.labelZh}>
          {action.labelEn}
        </a>
      ))}
    </nav>
  </details>
)}

<style>
  .detail-action-menu { position: relative; flex: 0 0 auto; }
  .detail-action-link, summary { display: grid; place-items: center; width: 30px; height: 30px; cursor: pointer; color: var(--gold); border: 1px solid var(--line); border-radius: 6px; list-style: none; }
  summary::-webkit-details-marker { display: none; }
  .detail-action-link:hover, .detail-action-link:focus-visible, summary:hover, summary:focus-visible, [open] summary { border-color: var(--gold); background: #ffffff0a; }
  .detail-action-link svg, summary svg { width: 17px; height: 17px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
  .detail-action-menu > .detail-action-list { position: absolute; z-index: 5; right: 0; top: calc(100% + 8px); width: min(250px, calc(100vw - 28px)); display: grid; grid-template-columns: 1fr; padding: 6px; border: 1px solid var(--line); border-radius: 8px; background: var(--panel2); box-shadow: 0 16px 36px #0008; }
  .detail-action-list > a { display: flex; width: 100%; min-height: 40px; align-items: center; padding: 10px 12px; border-radius: 5px; color: var(--text); font-size: .72rem; font-weight: 700; letter-spacing: .05em; overflow-wrap: anywhere; }
  .detail-action-list > a:hover, .detail-action-list > a:focus-visible { color: var(--gold); background: #ffffff0a; }
  @media(max-width:767px){.detail-action-link,summary{width:var(--touch-target);height:var(--touch-target)}.detail-action-list>a{min-height:var(--touch-target)}}
</style>

<script>
  const closeMenusOutside = (target?: Node | null) => {
    document.querySelectorAll<HTMLDetailsElement>('.detail-action-menu[open]').forEach((menu) => {
      if (!target || !menu.contains(target)) menu.removeAttribute('open');
    });
  };

  document.addEventListener('pointerdown', (event) => closeMenusOutside(event.target as Node));
</script>
