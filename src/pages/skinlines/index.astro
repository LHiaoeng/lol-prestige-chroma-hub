---
import BaseLayout from '../../layouts/BaseLayout.astro';
import RuntimeReferenceRoot from '../../components/RuntimeReferenceRoot.astro';
import { resolveLocale, type Locale } from '../../i18n/config';
import { createEntitySeo } from '../../seo/entity-seo';

const locale = (Astro.props.locale ?? resolveLocale(Astro.currentLocale)) as Locale;
const isZh = locale === 'zh-cn';
const seo = createEntitySeo('skinlines', undefined, isZh ? '皮肤系列资料' : 'Skinline references', isZh ? '在浏览器中读取皮肤系列资料。' : 'Browse skinline references in the browser.', locale);
---
<BaseLayout title={seo.title} description={seo.description} canonical={seo.canonical} jsonLd={seo.jsonLd} {locale}>
  <RuntimeReferenceRoot page="skinlines" mode="list" {locale} label={isZh ? '皮肤系列' : 'Skinlines'} heading={isZh ? '皮肤系列资料' : 'Skinline references'} lede={isZh ? '浏览皮肤系列名称。' : 'Browse skinline names.'} />
</BaseLayout>
